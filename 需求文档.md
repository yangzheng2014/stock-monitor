# 股票监控软件 —— 完整需求与技术文档（测试版 v0.1）

> 版本：v0.1（测试版）
> 编写日期：2026-08-14
> 状态：需求已细化，等待确认后进入开发
> 说明：本文档中所有"默认/建议"内容均为本文档作者补充的默认决策，可随时修改

---

## 目录

1. [项目概述](#一项目概述)
2. [总体架构](#二总体架构)
3. [数据源设计（免费 API 详解）](#三数据源设计免费-api-详解)
4. [功能需求明细](#四功能需求明细)
5. [界面设计（参考同花顺）](#五界面设计参考同花顺)
6. [技术方案](#六技术方案)
7. [测试计划](#七测试计划)
8. [开发计划](#八开发计划)
9. [风险与对策](#九风险与对策)
10. [版本路线图](#十版本路线图)
11. [待确认事项](#十一待确认事项)

---

## 一、项目概述

### 1.1 项目背景

个人投资者需要一个**轻量、免费、零成本**的股票监控工具，用于日常盯盘与复盘分析。
市面软件（同花顺、东方财富等）功能全面但界面复杂、广告多、数据虽免费但不开源可定制。
本项目目标是用免费公开数据接口，自建一个界面风格参考同花顺的监控软件，完全可控、可定制。

### 1.2 项目目标

| # | 目标 | 验收口径 |
|---|------|----------|
| G1 | 单只股票实时监控 | 界面实时显示现价、涨跌额、涨跌幅，自动刷新 |
| G2 | K 线图（蜡烛图） | 可切换最近 20 / 50 / 200 个交易日三个区间 |
| G3 | 双均线 | K 线上叠加两条均线，默认 MA5 / MA20 |
| G4 | 界面接近同花顺 | 深色主题、红涨绿跌、十字光标、成交量副图 |
| G5 | 全程免费 | 所有数据来自免费公开接口，无任何 key 或费用 |
| G6 | 测试版先跑通 | 以贵州茅台（600519）为默认标的验证全链路 |

### 1.3 非目标（本期明确不做）

- ❌ 多只股票同屏监控 / 自选股列表
- ❌ 股票交易 / 模拟盘下单
- ❌ MACD、KDJ、RSI、BOLL 等技术指标（列入扩展）
- ❌ 移动端 App
- ❌ 数据商业级实时性保证（秒级即可，不做毫秒级行情）

### 1.4 成功标准

测试版是否成功，用以下可量化标准衡量：

1. 从启动到看到完整 K 线图 ≤ 5 秒（含数据拉取）
2. 20/50/200 三个区间切换即时响应（≤ 1 秒）
3. K 线数据与同花顺同日对照误差在允许范围内（见 7.4 节）
4. 连续运行 30 分钟：无崩溃、无内存持续增长、界面不卡顿
5. 断网 1 分钟后界面有明确提示，恢复网络后 10 秒内自动恢复刷新

### 1.5 术语表

| 术语 | 含义 |
|------|------|
| K 线（蜡烛图） | 一根 K 线包含 开盘价(Open)、最高价(High)、最低价(Low)、收盘价(Close) 四个价格 |
| OHLC | Open/High/Low/Close 四价格的缩写 |
| 均线（MA） | Moving Average，N 日移动平均 = 最近 N 个交易日收盘价平均值 |
| MA5/MA20 | 5 日均线 / 20 日均线 |
| 前复权 | 以当前价格为基准，把历史价格按分红除权比例调整，K 线连续不跳空 |
| 涨跌幅 | (现价 − 昨收) ÷ 昨收 × 100% |
| 交易日 | 沪深交易所开市日（周一至周五，节假日休市） |
| 红涨绿跌 | 中国证券市场惯例：上涨显示红色，下跌显示绿色 |
| 数据源降级 | 主数据源失败时自动切换到备用数据源 |

---

## 二、总体架构

### 2.1 架构图

```
┌────────────────────────── 浏览器（Chrome/Edge/Safari）─────────────────────────┐
│                                                                               │
│  前端单页应用 index.html（ECharts）                                            │
│  ┌─────────────────────────────────────────────────────────────────────────┐  │
│  │ ① 实时行情栏（现价/涨跌/涨跌幅）        ② 工具栏（20/50/200 切换）      │  │
│  │ ③ K线主图（蜡烛+双均线）  ④ 成交量副图   ⑤ 状态栏（数据源/更新时间）    │  │
│  └─────────────────────────────────────────────────────────────────────────┘  │
└───────────────────────────────▲──────────────────────────────────────────────┘
                                │ HTTP (localhost:8765)
┌───────────────────────────────┴──────────── 后端（Python + Flask）────────────┐
│                                                                               │
│  routes.py  (REST API)          datasource/  (数据源适配器)                    │
│  ├─ /api/config                ├─ eastmoney.py  (东方财富：历史K线+实时)       │
│  ├─ /api/realtime              ├─ sina.py       (新浪：实时行情)               │
│  ├─ /api/kline                 ├─ tencent.py    (腾讯：实时行情，备用)         │
│  └─ /api/health                └─ manager.py    (降级切换/节流/缓存)           │
│                                                                               │
│  core/                          data/  (本地缓存)         logs/ (日志)        │
│  ├─ indicator.py (均线计算)     └─ kline_sh600519.csv    └─ app.log           │
│  └─ kline.py (数据模型/校验)                                                   │
└───────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 模块职责

| 模块 | 职责 | 依赖 |
|------|------|------|
| `run.py` | 启动 Flask 服务，自动打开浏览器 | Flask, webbrowser |
| `routes.py` | 提供 4 个 REST API，聚合数据 | datasource, core |
| `datasource/eastmoney.py` | 东方财富历史 K 线 + 实时行情 | requests |
| `datasource/sina.py` | 新浪实时行情（复用现有已验证代码） | requests |
| `datasource/tencent.py` | 腾讯实时行情（备用） | requests |
| `datasource/manager.py` | 数据源优先级与降级切换、请求节流、失败统计 | 以上三者 |
| `core/indicator.py` | MA 均线计算 | pandas |
| `core/kline.py` | K 线数据校验、复权校验、区间切片 | pandas |
| `config.json` | 全部可配置项 | — |
| `static/index.html` | 前端页面（ECharts 绘图 + 定时刷新） | ECharts CDN(本地化) |

### 2.3 数据流

```
启动 → 读 config.json → 缓存是否存在？
  ├─ 是 → 读取本地缓存 CSV（秒开）
  ├─ 否 → 东财拉取最近 250 交易日日 K（前复权）→ 校验 → 落缓存
  ↓
计算 MA5/MA20 → 前端渲染 K 线 + 均线 + 成交量
  ↓
每 5 秒 → 实时行情接口 → 顶部行情栏刷新 + 盘中更新最后一根 K 线
每日 15:10（收盘后）→ 重新拉取当日完整日 K 覆盖缓存
```

---

## 三、数据源设计（免费 API 详解）

### 3.1 数据源总览与选型决策

| 优先级 | 数据源 | 用途 | 优点 | 缺点 |
|--------|--------|------|------|------|
| 主 | **东方财富 push2 接口** | 历史日 K + 实时行情 | 稳定、直接支持前复权参数、无 key | 公开接口可能调整，需维护 |
| 备1 | **新浪财经 hq.sinajs.cn** | 实时行情 | 现有代码已验证可用 | 需带 Referer、GBK 编码 |
| 备2 | **腾讯财经 qt.gtimg.cn** | 实时行情 | 字段丰富 | GBK 编码 |
| 聚合 | AkShare（可选封装） | 兜底 | 一个库封装多个源 | 引入额外依赖，测试版不采用 |

> **决策说明**：
> - 历史 K 线只用**东方财富**（唯一支持前复权参数 fqt=1 的免费源，避免本地复权计算的误差）。
> - 实时行情走"东财 → 新浪 → 腾讯"三级降级。
> - 不引入 AkShare，直接 HTTP 请求，减少依赖、便于控制限流。

### 3.2 东方财富 —— 历史日 K 线（主数据源）

**接口**：`GET https://push2his.eastmoney.com/api/qt/stock/kline/get`

**请求参数**：

| 参数 | 值 | 说明 |
|------|----|------|
| secid | `1.600519` | 1=沪市，0=深市；格式 `{市场}.{代码}` |
| klt | `101` | K 线类型：101=日K |
| fqt | `1` | 复权：0=不复权，1=前复权，2=后复权 |
| beg | `20250101` | 起始日期（YYYYMMDD） |
| end | `20500101` | 结束日期，写远日期即可取全 |
| fields1 | `f1,f2,f3,f4,f5,f6` | 固定 |
| fields2 | `f51,f52,f53,f54,f55,f56,f57,f58` | f51日期 f52开 f53收 f54高 f55低 f56量 f57额 f58振幅 |

**响应结构**（示例，价格单位为元）：

```json
{
  "rc": 0,
  "data": {
    "code": "600519",
    "name": "贵州茅台",
    "klines": [
      "2026-01-05,1420.00,1430.50,1435.00,1410.00,26800,3800000000,1.76",
      "2026-01-06,1432.00,1440.00,1448.00,1425.00,31200,4470000000,1.61"
    ]
  }
}
```

每条 kline 为逗号分隔字符串，字段顺序对应 fields2：`日期,开,收,高,低,成交量(手),成交额(元),振幅%`。

**解析注意**：`f52`（开）在前、`f53`（收）紧随其后，实际顺序为 **date, open, close, high, low, volume, amount, amplitude**，与通常 OHLC 顺序不同，务必按此顺序解析，不能想当然按 OHLC。

**限流要求**：日 K 数据每天至多更新 2 次（启动 1 次 + 收盘 1 次），不得频繁请求。

### 3.3 东方财富 —— 实时行情（主数据源）

**接口**：`GET https://push2.eastmoney.com/api/qt/stock/get`

**请求参数**：

| 参数 | 值 |
|------|----|
| secid | `1.600519` |
| fields | `f43,f44,f45,f46,f47,f48,f57,f58,f60,f169,f170` |

**字段含义**：

| 字段 | 含义 | 注意 |
|------|------|------|
| f43 | 最新价 | **需按 f59 精度缩放**，一般 ÷100（如 150032 → 1500.32） |
| f44 | 最高 | 同上缩放 |
| f45 | 最低 | 同上缩放 |
| f46 | 今开 | 同上缩放 |
| f60 | 昨收 | 同上缩放 |
| f169 | 涨跌额 | 同上缩放 |
| f170 | 涨跌幅 % | 已带两位小数 |
| f57/f58 | 代码/名称 | — |
| f86 | 时间戳 | 毫秒 |

**响应示例**：

```json
{
  "data": {
    "f43": 150032, "f57": "600519", "f58": "贵州茅台",
    "f60": 149000, "f169": 1032, "f170": 0.69
  }
}
```

**注意**：`f43` 等价格字段在停牌或异常时为 `"-"`，需判空后再转换。

### 3.4 新浪财经 —— 实时行情（备用）

**接口**：`GET https://hq.sinajs.cn/list=sh600519`

- 必须携带请求头 `Referer: https://finance.sina.com.cn/`，否则返回 403
- 响应为 **GBK 编码**，需 `resp.content.decode("gbk")`
- 返回格式（逗号分隔，A 股）：

```
var hq_str_sh600519="贵州茅台,今开,昨收,当前价,最高,最低,买一价,卖一价,成交量,成交额,...
```

| 索引 | 含义 |
|------|------|
| 0 | 名称 |
| 1 | 今开 |
| 2 | 昨收 |
| 3 | **当前价** |
| 4 | 最高 |
| 5 | 最低 |
| 8 | 成交量（股） |
| 9 | 成交额（元） |
| 30 | 日期 |
| 31 | 时间 |

> 该接口实现已在本仓库 `股票监控/main.py` 中验证通过，直接迁移复用。

### 3.5 腾讯财经 —— 实时行情（第二备用）

**接口**：`GET https://qt.gtimg.cn/q=sh600519`

- 响应为 GBK 编码
- 返回：`v_sh600519="1~贵州茅台~600519~当前价~昨收~今开~成交量~外盘~内盘~买一~...~涨跌~涨跌幅~..."`

| 索引 | 含义 |
|------|------|
| 3 | 当前价 |
| 4 | 昨收 |
| 5 | 今开 |
| 6 | 成交量（手） |
| 31 | 涨跌 |
| 32 | 涨跌幅 % |
| 30 | 时间 |

### 3.6 数据源降级策略（manager.py）

```
状态机：每一数据源维护 [连续失败次数 fail_count]

实时行情请求流程：
1. 按优先级取数据源列表 [eastmoney, sina, tencent]
2. 请求优先级最高的源
   ├─ 成功 → 重置 fail_count=0，返回数据
   ├─ 失败 → fail_count++，若 fail_count >= 2 → 降级到下一优先级（并告警日志）
   └─ 全部失败 → 返回"数据源异常"，前端显示缓存中最后一次有效数据 + 状态栏警示
3. 每 60 秒自动尝试恢复优先级的源（fail_count 减半）
```

### 3.7 请求节流与缓存

| 项目 | 策略 |
|------|------|
| 实时行情 | 两次请求同一源间隔 ≥ 3 秒（后端强制节流） |
| 历史 K 线 | 启动时 1 次 + 收盘后 1 次；文件缓存有效期 1 天 |
| 文件缓存 | `data/kline_sh600519.csv`，启动优先读缓存（秒开），后台异步更新 |
| 限流规避 | 单源失败时切源而不是重试轰炸；连续失败进入 30 秒静默期 |
| User-Agent | 模拟浏览器 UA；东财接口一般无 UA 校验，新浪必须带 Referer |

### 3.8 数据校验（core/kline.py）

对拉取到的每一根 K 线做以下校验，不合法则丢弃并记日志：

| 校验项 | 规则 |
|--------|------|
| 价格为正 | open/high/low/close > 0 |
| OHLC 逻辑 | low ≤ min(open, close) 且 high ≥ max(open, close) |
| 涨跌幅合理 | 单日 |涨跌幅| ≤ 12%（主板涨跌停 10%，留容差；科创/创业为 20%） |
| 成交量 | volume ≥ 0 |
| 日期合法 | 日期为交易日（工作日），且递增去重 |
| 数量 | 连续 3 根以上数据非法 → 判定数据源异常，触发降级 |

---

## 四、功能需求明细

> 优先级：P0=必须有，P1=尽量有，P2=可延后

### 4.1 股票配置与监控

| 编号 | 优先级 | 需求 | 详细说明 |
|------|--------|------|----------|
| F1.1 | P0 | 单只股票监控 | 默认监控贵州茅台 sh600519；代码可在 config.json 修改 |
| F1.2 | P0 | 支持代码格式 | 支持 `sh600519` / `600519.SH` / `600519` 三种输入自动归一化 |
| F1.3 | P0 | 股票元信息 | 显示股票名称、代码、所属市场（沪/深） |
| F1.4 | P1 | 监控其他股票 | 命令行参数 `python run.py 000001` 可覆盖默认标的（本期仅支持单只） |

### 4.2 实时行情显示

| 编号 | 优先级 | 需求 | 详细说明 |
|------|--------|------|----------|
| F2.1 | P0 | 现价显示 | 顶部醒目显示当前最新价，红色/绿色按涨跌着色 |
| F2.2 | P0 | 涨跌额与涨跌幅 | 显示 `+10.32 (+0.69%)` 格式，涨红跌绿 |
| F2.3 | P0 | 关键价格 | 显示今开、最高、最低、昨收、成交量、成交额 |
| F2.4 | P0 | 自动刷新 | 默认 5 秒刷新一次，interval 可配置（最短 3 秒） |
| F2.5 | P1 | 盘中 K 线联动 | 交易日盘中，最新一根 K 线的 open/high/low/close 随实时价更新 |
| F2.6 | P1 | 涨跌停高亮 | 触及涨停/跌停时显示"涨停/跌停"标签 |

**涨跌幅计算公式**：`涨跌幅 = (现价 − 昨收) / 昨收 × 100%`，保留两位小数。
**颜色判定**：现价 > 昨收 → 红；现价 < 昨收 → 绿；相等 → 白/灰。

### 4.3 K 线图

| 编号 | 优先级 | 需求 | 详细说明 |
|------|--------|------|----------|
| F3.1 | P0 | 蜡烛图绘制 | 每根 K 线展示 OHLC；实心/空心规则：涨=红实心，跌=绿实心（同花顺风格） |
| F3.2 | P0 | 上影/下影线 | 最高/最低价通过上下影线表现 |
| F3.3 | P0 | 区间切换 | 工具栏三个按钮：**20 日 / 50 日 / 200 日**，点击即时切换 |
| F3.4 | P0 | 默认区间 | 默认显示最近 50 日 |
| F3.5 | P0 | 数据充足性 | 后台始终拉取 250 个交易日数据，保证 200 日区间完整 |
| F3.6 | P1 | 滚动缩放 | 支持鼠标滚轮缩放 K 线区域（ECharts dataZoom） |
| F3.7 | P1 | 区间平移 | 支持拖拽平移查看区间外的历史 K 线 |
| F3.8 | P2 | 周K/月K | 本期不做，列入扩展 |

**区间含义**：20/50/200 日 = 最近 20/50/200 个**交易日**（自然日历月约对应 1/2.5/10 个月）。

### 4.4 双均线

| 编号 | 优先级 | 需求 | 详细说明 |
|------|--------|------|----------|
| F4.1 | P0 | 两条均线 | 默认 **MA5 + MA20**，在 K 线主图以折线叠加 |
| F4.2 | P0 | 均线颜色 | MA5=黄色，MA20=紫色（参考同花顺），图例显示当前值 |
| F4.3 | P0 | 参数可配置 | config.json 的 `ma` 数组可改为任意双周期，如 [10, 30] |
| F4.4 | P1 | 数据不足处理 | 前 n-1 根 K 线无法计算均线时该点留空（不画线），不做前补 |
| F4.5 | P1 | 金叉死叉提示 | 均线交叉时在图中标记"金叉/死叉"小箭头（P2 可后移） |

**均线算法**：
```
MA(n)[t] = (Close[t-n+1] + Close[t-n+2] + ... + Close[t]) / n
```
实现：`pandas.Series.rolling(n).mean()`，前 n-1 个值为 NaN 不绘制。

### 4.5 成交量副图

| 编号 | 优先级 | 需求 | 详细说明 |
|------|--------|------|----------|
| F5.1 | P0 | 成交量柱状图 | K 线主图下方独立区域，与主图按日期对齐 |
| F5.2 | P0 | 红涨绿跌 | 当日上涨柱=红色，下跌柱=绿色 |
| F5.3 | P0 | 随区间联动 | 20/50/200 切换时副图同步缩放 |
| F5.4 | P1 | 量价提示 | 悬停显示当日成交量、成交额 |
| F5.5 | P2 | 成交量均线 | 5 日均量线（VOL5），扩展阶段做 |

### 4.6 十字光标与数据提示

| 编号 | 优先级 | 需求 | 详细说明 |
|------|--------|------|----------|
| F6.1 | P0 | 十字光标 | 鼠标悬停主图显示十字交叉参考线（ECharts axisPointer: cross） |
| F6.2 | P0 | Tooltip | 悬停显示当日：开、高、低、收、涨跌幅、成交量、均线值 |
| F6.3 | P0 | Tooltip 样式 | 深色半透明背景、白色文字，K 线信息红涨绿跌配色 |
| F6.4 | P1 | 跟随鼠标 | 十字线随鼠标移动实时更新 |

### 4.7 状态栏与错误提示

| 编号 | 优先级 | 需求 | 详细说明 |
|------|--------|------|----------|
| F7.1 | P0 | 更新时间 | 状态栏显示"最后更新 HH:MM:SS" |
| F7.2 | P0 | 数据源状态 | 显示当前生效数据源（东财/新浪/腾讯/缓存） |
| F7.3 | P0 | 异常提示 | 数据源全部失败时状态栏红字警示"数据源异常，显示缓存数据" |
| F7.4 | P0 | 交易状态 | 显示"交易中 / 午间休市 / 已收盘 / 休市日" |
| F7.5 | P1 | 断网恢复 | 网络恢复后自动继续刷新，无需手动操作 |

### 4.8 交易时段处理

| 时段 | 时间（北京时间） | 行为 |
|------|------------------|------|
| 集合竞价 | 09:15–09:25 | 显示竞价价，5 秒刷新 |
| 早盘 | 09:30–11:30 | 正常 5 秒刷新 |
| 午休 | 11:30–13:00 | 30 秒低频刷新（价格不变，防止无意义请求） |
| 午后 | 13:00–15:00 | 正常 5 秒刷新 |
| 收盘后 | 15:00 之后 | 15:10 拉取最终日 K 覆盖缓存，之后停止实时刷新 |
| 周末/节假日 | 全天 | 只显示缓存数据，状态栏标"休市日" |

**节假日判断（简化方案）**：测试版按"工作日即交易"处理，并在状态栏提示"节假日可能休市，请人工确认"。
（扩展阶段引入交易日历库 `exchange_calendars`。）

---

## 五、界面设计（参考同花顺）

### 5.1 整体布局

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ ① 顶部行情栏（高 48px）                                                       │
│   贵州茅台 600519  [沪]   现价 1500.32  ▲ +10.32  +0.69%   涨停  | 今开 1485   │
│   昨收 1490.00 | 最高 1510.00 | 最低 1480.00 | 量 2.8万手 | 额 42.1亿          │
├─────────────────────────────────────────────────────────────────────────────┤
│ ② 工具栏（高 40px）                                                           │
│   [日K]  区间: (20日) (50日) (200日)    均线: [MA5] [MA20]    刷新: 5s        │
├──────────────────────────────┬──────────────────────────────────────────────┤
│ ③ 主图区  K线+均线            │        图例：MA5 1498.20 ──  MA20 1465.31    │
│  （高 ≈ 60%）                 │                                             │
│                              │  ← 十字光标 + Tooltip 悬停效果                │
│  价格轴 Y1                    │                                             │
├──────────────────────────────┴──────────────────────────────────────────────┤
│ ④ 成交量副图（高 ≈ 25%）   红柱=涨 绿柱=跌，与主图日期对齐                    │
├─────────────────────────────────────────────────────────────────────────────┤
│ ⑤ 状态栏（高 24px）                                                           │
│  数据源: 东方财富 | 交易中 | 最后更新 14:23:05 | 数据延迟 ≤5s                  │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 5.2 视觉规范（默认深色主题）

| 项目 | 值 | 说明 |
|------|----|------|
| 窗口背景 | `#1E1E1E` | 同花顺深色风格 |
| 图表背景 | `#1E1E1E` | K 线区纯深色 |
| 网格线 | `#2A2A2A` 虚线 | 轻微可见 |
| 主文字 | `#CCCCCC` | 行情数字用等宽字体 |
| **上涨红** | `#EF232A` | 同花顺红涨 |
| **下跌绿** | `#14B143` | 同花顺绿跌 |
| 平盘 | `#999999` | 白灰 |
| K线上影线/边框 | 涨红跌绿 | 边框与实体同色 |
| MA5 | `#F9D71C` 黄色 | — |
| MA20 | `#A967FF` 紫色 | — |
| 成交量柱 | 涨红跌绿 | — |
| 十字光标 | `#888888` 细线 | 半透明 |
| Tooltip | 背景 `rgba(0,0,0,0.85)` | 白字 |
| 字体 | `PingFang SC, Microsoft YaHei, sans-serif` | 数字用 `DIN, Roboto Mono` 等宽 |
| 窗口尺寸 | 默认 1280×800 | 可自由缩放，组件自适应 |

> 浅色主题（P2）：同结构反色，背景 `#FFFFFF`，文字 `#333333`。

### 5.3 组件说明

| 组件 | 说明 |
|------|------|
| 顶部行情栏 | 单行信息：名称+代码 | 现价（大号加粗着色）| 涨跌额+涨跌幅（着色）| 今开/昨收/最高/最低/量/额（次要灰字） |
| 区间按钮 | 三个按钮，选中态高亮（同花顺为底部蓝条/白底） |
| 主图 | ECharts candlestick 系列 + 两条 line 系列（MA5/MA20），Y 轴右侧可选显示均线刻度 |
| 副图 | bar 系列，`xAxis` 与主图共享（`grid` 分区），tooltip 联动 |
| 图例 | 右上角，显示均线名称与当前值，可点击显隐 |

### 5.4 交互规范

| 操作 | 行为 |
|------|------|
| 悬停 | 显示十字光标 + Tooltip（日期/OHLC/涨跌幅/量/额/MA值） |
| 点击 20/50/200 | 立即切换显示区间，十字光标消失 |
| 滚轮 | 缩放 K 线时间范围 |
| 拖拽 | 平移查看区间外历史 |
| 点击均线图例 | 显示/隐藏该均线 |
| 刷新按钮（可选） | 手动立即刷新实时行情 |

### 5.5 红涨绿跌一致性要求

**全应用统一**：顶部行情栏、K 线蜡烛、成交量柱、Tooltip 数值、涨跌幅标签，
一律使用同一颜色规则（涨红 `#EF232A` / 跌绿 `#14B143`），不允许局部出现相反配色。

---

## 六、技术方案

### 6.1 技术选型与理由

| 层面 | 选型 | 版本 | 理由 |
|------|------|------|------|
| 后端语言 | Python | 3.10+ | 与现有代码一致 |
| Web 框架 | Flask | 3.x | 轻量、单文件即可启动 |
| 数据请求 | requests | 2.x | 现有依赖 |
| 数据处理 | pandas | 2.x | 均线计算、CSV 缓存 |
| 图表前端 | **ECharts** | 5.x | K 线组件成熟，十字光标/缩放/tooltip 开箱即用，视觉最接近同花顺 |
| 前端构建 | 纯静态 HTML+JS | — | 单页无构建工具，零前端工程负担 |
| ECharts 引入 | 本地文件 `echarts.min.js` | 5.x | 离线可用，不依赖 CDN |
| 日志 | logging（标准库） | — | 滚动文件 logs/app.log |
| 打包（P2） | PyInstaller | — | 出单文件 exe/app |

> **为什么选 ECharts 而不是 matplotlib**：matplotlib 实现同花顺式交互（十字光标、区间切换动画、
> Tooltip、缩放）工作量大且效果一般；ECharts 这些能力开箱即用，且最终界面美观度最接近同花顺。
> 现有 `main.py` 的新浪抓取逻辑将迁移为 `datasource/sina.py` 复用。

### 6.2 后端 API 定义

**Base URL**: `http://localhost:8765`

| 接口 | 方法 | 参数 | 返回 | 说明 |
|------|------|------|------|------|
| `/` | GET | — | `static/index.html` | 前端页面 |
| `/api/config` | GET | — | 当前配置 | 股票、均线、区间、刷新间隔 |
| `/api/realtime` | GET | — | 实时行情 JSON | 见下方示例 |
| `/api/kline` | GET | `days=250` | 历史 K 线 + 均线 | days ≤ 250 |
| `/api/health` | GET | — | 各数据源状态 | 用于状态栏 |

**`/api/realtime` 返回示例**：

```json
{
  "code": "sh600519",
  "name": "贵州茅台",
  "price": 1500.32,
  "prev_close": 1490.00,
  "open": 1485.00,
  "high": 1510.00,
  "low": 1480.00,
  "change": 10.32,
  "pct": 0.69,
  "volume": 28000,
  "amount": 4210000000.0,
  "timestamp": "2026-08-14 14:23:05",
  "source": "eastmoney",
  "trade_status": "trading",
  "cached": false
}
```

**`/api/kline` 返回示例**：

```json
{
  "code": "sh600519",
  "name": "贵州茅台",
  "fqt": "qfq",
  "count": 250,
  "dates": ["2026-01-05", "2026-01-06"],
  "ohlc": [
    {"date": "2026-01-05", "open": 1420.00, "close": 1430.50, "low": 1410.00, "high": 1435.00, "volume": 26800},
    {"date": "2026-01-06", "open": 1432.00, "close": 1440.00, "low": 1425.00, "high": 1448.00, "volume": 31200}
  ],
  "ma5": [null, null, null, null, 1432.10],
  "ma20": [null],
  "updated_at": "2026-08-14 15:11:02"
}
```

> 注：ohlc 元素顺序按 ECharts candlestick 要求的 `[open, close, low, high]` 返回，前端无需再调整。

### 6.3 前端设计要点（index.html）

```
- 顶部行情栏：每 5 秒 fetch('/api/realtime') 更新文本与颜色
- ECharts 实例：
  ├─ grid[0]: 主图（top 12%, height 55%）
  ├─ grid[1]: 成交量（top 72%, height 20%）
  ├─ xAxis 两条 axisIndex 对应两个 grid，共享 data 日期
  ├─ yAxis[0]: 价格轴（主图）；yAxis[1]: 成交量轴（副图，显示 '万手' 格式化）
  ├─ series:
  │   ├─ candlestick: data = ohlc，itemStyle.color=#EF232A, color0=#14B143,
  │   │              borderColor 同上（红涨绿跌）
  │   ├─ line MA5: yellow, symbol: 'none'
  │   ├─ line MA20: purple, symbol: 'none'
  │   └─ bar volume: 按当日涨跌分别着色（data 中每根带 itemStyle）
  ├─ axisPointer: { type: 'cross', link: [{xAxisIndex: 'all'}] }
  ├─ dataZoom: 滚轮缩放 + 拖拽平移（inside + slider）
  └─ tooltip: trigger 'axis'，自定义 formatter 显示 OHLC/涨跌幅/量额/MA 值
- 区间切换：点击 20/50/200 → 用后端数据 slice 前 N 根 → setOption 刷新
- 网络异常：fetch 失败 3 次 → 状态栏红字提示；成功后恢复
```

**盘中联动细节**：交易时段内，将实时价格合并到 K 线最后一根：
`last.close = price; last.high = max(last.high, price); last.low = min(last.low, price)`。

### 6.4 配置项（config.json 完整示例）

```json
{
  "symbol": "600519",
  "market": "sh",
  "name": "贵州茅台",
  "kline_ranges": [20, 50, 200],
  "default_range": 50,
  "history_days": 250,
  "ma": [5, 20],
  "ma_colors": { "5": "#F9D71C", "20": "#A967FF" },
  "refresh_interval_sec": 5,
  "close_refresh_min": 3,
  "history_refresh_interval_sec": 3600,
  "cache_file": "data/kline_{symbol}.csv",
  "cache_ttl_sec": 86400,
  "port": 8765,
  "theme": "dark",
  "realtime_sources": ["eastmoney", "sina", "tencent"],
  "history_source": "eastmoney",
  "fail_threshold": 2,
  "retry_interval_sec": 60,
  "log_file": "logs/app.log"
}
```

### 6.5 错误处理与日志

| 场景 | 处理 |
|------|------|
| 网络超时 | 10 秒超时，重试 1 次，仍失败计 fail_count |
| JSON 解析失败 | 记日志，计失败 |
| 字段缺失/为 "-" | 判空后视为无效数据，计失败 |
| 数据校验不过 | 丢弃该 K 线并告警日志（连续 3 根则切源） |
| 缓存损坏 | 删除缓存文件重新拉取 |
| 前端 API 失败 | 3 次连续失败显示警示，不中断页面 |
| 全部日志 | `logs/app.log` 滚动 10MB×3，格式 `[时间] [级别] [模块] 消息` |

### 6.6 运行方式

```bash
cd /Users/yangzheng/cc-项目/股票监控

# 安装依赖（requirements.txt 新增 flask）
pip install -r requirements.txt

# 启动（自动打开浏览器 http://localhost:8765）
python run.py

# 指定监控其他股票
python run.py 000001        # 深市平安银行
python run.py 600000        # 沪市浦发银行
```

---

## 七、测试计划

### 7.1 单元测试（pytest）

| 测试文件 | 用例 | 断言要点 |
|----------|------|----------|
| `tests/test_indicator.py` | MA5 计算 | 构造 10 个已知收盘价，手算 MA5 对照（含末尾 NaN 规则） |
| `tests/test_indicator.py` | MA 周期不足 | 前 4 根 MA5 为 None，第 5 根起有值 |
| `tests/test_indicator.py` | 空数据 | 空序列不抛异常，返回全 None |
| `tests/test_kline.py` | 区间切片 | 250 根切片 20/50/200 数量正确 |
| `tests/test_kline.py` | OHLC 校验 | 非法数据（high<low 等）被拒绝 |
| `tests/test_kline.py` | 日期去重升序 | 乱序/重复日期被修正 |
| `tests/test_datasource.py` | 东财 K 线解析 | mock 响应按 f51~f58 顺序解析正确 |
| `tests/test_datasource.py` | 东财实时解析 | mock f43=150032 → 1500.32（精度处理） |
| `tests/test_datasource.py` | 新浪解析 | mock GBK 内容，取索引 3 价格 |
| `tests/test_datasource.py` | 降级切换 | 主源连续失败 2 次后使用备源 |
| `tests/test_datasource.py` | 降级恢复 | 60 秒后恢复优先级源 |
| `tests/test_datasource.py` | 异常字段 | 返回 "-" 时判为失败 |
| `tests/test_api.py` | /api/kline | 返回结构字段齐全、ma 长度与 kline 一致 |
| `tests/test_api.py` | /api/realtime | 字段齐全、价格精度两位小数 |

### 7.2 数据正确性对照（对照同花顺）

**方法**：收盘后（15:10 后）取贵州茅台最近 5 个交易日数据，与同花顺 App 日 K 页面对照：

| 对照项 | 容差 |
|--------|------|
| open/close/high/low | ≤ 0.02 元（前复权口径，需同花顺也选"前复权"） |
| 成交量 | ≤ 1% 相对误差 |
| MA5/MA20 数值 | ≤ 0.05 元 |
| 现价/涨跌幅 | 与同花顺实时页一致 |

> 若发现系统性偏差（如复权口径差异），优先检查 fqt 参数与同花顺设置是否一致。

### 7.3 集成测试（手工脚本）

| 场景 | 步骤 | 预期 |
|------|------|------|
| 冷启动 | 删除缓存，启动程序 | ≤5 秒出图，状态栏"东方财富" |
| 二次启动 | 保留缓存，启动程序 | 立即出图，后台更新 |
| 区间切换 | 点击 20/50/200 | 图表即时切换，副图同步 |
| 悬停 | 鼠标移过 K 线 | 十字光标+完整 Tooltip |
| 断网 | 关闭 Wi-Fi 1 分钟 | 状态栏红字警示，界面保留最后数据 |
| 恢复 | 打开 Wi-Fi | ≤10 秒恢复刷新 |
| 午休 | 11:30 后观察 | 刷新变 30 秒低频，状态"午间休市" |
| 收盘后 | 15:10 后 | 缓存更新为当日完整 K 线，停止实时刷新 |
| 涨跌颜色 | 观察下跌日 | 蜡烛/成交量/顶部行情全为绿色，无红绿错乱 |

### 7.4 验收标准（总）

1. 启动 ≤5 秒显示贵州茅台完整 K 线
2. 20/50/200 日切换 ≤1 秒响应
3. 与同花顺对照数据误差在 7.2 节容差内
4. 实时刷新 5 秒/次，盘中最后一根 K 线随现价联动
5. 数据源降级、断网恢复行为符合 7.3 节预期
6. 连续运行 30 分钟无崩溃、无内存增长（psutil 检测）

---

## 八、开发计划

| 阶段 | 任务 | 产出 | 依赖 | 预计 |
|------|------|------|------|------|
| P0 数据层 | 实现 eastmoney.py（历史 K 线 + 实时） | 可拉取茅台日 K 与现价 | 无 | 0.5 天 |
| P0 数据层 | 迁移 sina.py（复用 main.py 代码） | 备用实时源 | P0-东财 | 0.5 天 |
| P0 数据层 | 实现 tencent.py | 第二备用源 | 无 | 0.5 天 |
| P0 数据层 | 实现 manager.py 降级+节流+缓存 | 数据源可切换、CSV 缓存 | 以上 | 0.5 天 |
| P1 核心计算 | indicator.py 均线 + kline.py 校验切片 | MA5/MA20、区间切片 | P0 | 0.5 天 |
| P2 后端 | Flask 路由 + /api/config /api/realtime /api/kline /api/health | 后端 API | P0/P1 | 0.5 天 |
| P3 前端 | index.html + ECharts 主图/副图/区间切换/十字光标 | 完整界面 | P2 | 1 天 |
| P4 联调 | 实时刷新联动、盘中 K 线联动、交易时段逻辑 | 全功能闭环 | P3 | 0.5 天 |
| P5 健壮性 | 日志、异常处理、断网恢复、状态栏 | 稳定运行 | P4 | 0.5 天 |
| P6 测试 | 单元测试 + 对照同花顺 + 集成场景 | 测试报告 | 全部 | 1 天 |
| P7 收尾 | 文档整理、README、运行说明 | 交付物 | P6 | 0.5 天 |

**总计约 6~7 个工作日**，先交付 P0~P4（约 4 天）即达"可用的测试版"。

---

## 九、风险与对策

| 风险 | 等级 | 说明 | 对策 |
|------|------|------|------|
| 免费接口变动 | 高 | 东财/新浪接口字段或路径可能调整 | 三级数据源降级；数据源模块单点封装，接口变更只改一处 |
| 请求被限流/封禁 | 中 | 高频轮询可能触发风控 | 最低 3 秒间隔；节流器统一管控；失败退避 30 秒；日 K 每天最多 2 次 |
| 复权口径差异 | 中 | 前复权数值与同花顺可能差几毛钱 | 统一 fqt=1；对照测试确认；容差 0.02 元 |
| 节假日误判 | 中 | 工作日判断无假期表 | 测试版明示"请人工确认"；扩展版引入交易日历库 |
| ECharts 本地化失败 | 低 | CDN 不可达 | echarts.min.js 下载到 static/ 本地引入 |
| 数据准确性 | 低 | 免费源与同花顺小数位差异 | 对照容差机制 + 数据校验丢弃异常值 |
| 浏览器兼容 | 低 | 旧浏览器不支持 | 要求 Chrome/Edge 等现代浏览器 |
| 性能 | 低 | 250 根 K 线重绘 | ECharts 原生性能足够；限制渲染根数 ≤250 |

---

## 十、版本路线图

| 版本 | 内容 | 时间 |
|------|------|------|
| **v0.1（本期）** | 单股 + K线 + 双均线 + 20/50/200 + 成交量 + 实时刷新 | 2026-08 |
| v0.2 | 多指标（MACD/KDJ/BOLL/RSI）、MA5/10/20/60 四均线、周/月 K | 2026-09 |
| v0.3 | 多股票自选列表、多标签页、价格预警（阈值提醒） | 2026-10 |
| v0.4 | 分时图 + 五日分时、资金流向、数据 SQLite 本地库、离线看盘 | 2026-11 |
| v1.0 | PyInstaller 打包、浅色主题、交易日历、发布 | 2026-12 |

---

## 十一、待确认事项

> 以下问题本文档已给出**默认决策**，如无异议按默认执行；有异议请指出后修改。

| # | 问题 | 默认决策 |
|---|------|----------|
| 1 | 双均线参数？ | MA5 + MA20（可改 config.json） |
| 2 | 界面形态？ | 本地 Web + ECharts（浏览器打开 localhost），非桌面原生 |
| 3 | 20/50/200 日含义？ | 最近 N 个交易日 |
| 4 | 现有 main.py 折线图？ | 保留文件，实时抓取逻辑迁移复用，界面被新版取代 |
| 5 | 历史数据范围？ | 拉取 250 个交易日（保证 200 日完整） |
| 6 | 数据源优先级？ | 东财 → 新浪 → 腾讯 |
| 7 | 深色主题为默认？ | 是，浅色主题 P2 再做 |
| 8 | 是否需要中文名称支持其他股票？ | 名称由数据源自动返回，无需配置 |
| 9 | 是否需要模拟盘/买卖点提示？ | 本期不做 |
